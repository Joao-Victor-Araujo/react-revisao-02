import React from "react";
import './Header.css'

export default function Header(){
    return(
        <header data-testid="header">
            <a href="#">Home</a>
            <a href="#">Perfil</a>
        </header>
    )
}
